// Site-wide identity strings. The shell (home, about, listings, RSS,
// chrome) is English-only; only individual articles carry a language.
// Lifting these out of components means renaming the site is a
// one-file change.
export const SITE_NAME = 'garaemon';

export const SITE_DESCRIPTION =
  'A machine that consumes pop culture and outputs code.';

export const RSS_TITLE = SITE_NAME;
